spring:
  application:
    name: licensingservice
  profiles:
    active:
      default
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        service-id: configserver
        enabled: true
