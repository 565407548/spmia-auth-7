spring:
  application:
    name: licensing-service
  profiles:
    active:
      default
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        service-id: config-service
        enabled: true
